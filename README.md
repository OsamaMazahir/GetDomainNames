
## work
1. make changes
2. `git add .`
3. `git commit -m "......"`
4. `git push`

## publish
1. `npm run build`
2. `npm run deploy` Pushes the `\dist` into the `gh-pages` branch, and the live Add-in in Outlook updates automatically.
